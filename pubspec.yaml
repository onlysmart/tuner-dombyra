name: tuner_dombyra
description: "Dombyra Tuner - Tune your dombyra with real-time pitch detection."
publish_to: "none"
version: 1.3.0+4

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  onlysmart_ui:
    path: ../../../packages/onlysmart_ui
  flutter_localizations:
    sdk: flutter

  provider: ^6.1.2
  shared_preferences: ^2.3.3
  permission_handler: ^11.3.1
  record: ^5.2.0
  flutter_animate: ^4.5.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependency_overrides:
  record_linux: ^1.3.0

flutter:
  uses-material-design: true
